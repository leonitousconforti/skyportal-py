Add typed endpoint functions for allocations, follow-up requests, streams,
and observing runs, plus update and delete functions for sources, comments,
annotations, classifications, photometry, and spectra, all bound as methods
on the `SkyPortal` client.
